[pytest]
testpaths = tests
# tests/ on the path so conftest helpers are importable; repo root so the
# packages under test (training/, data_pipeline/) resolve as they do in the pipeline.
pythonpath = . tests
addopts = -q --strict-markers
markers =
    slow: end-to-end pipeline run; minutes rather than seconds. Deselect with -m "not slow".
